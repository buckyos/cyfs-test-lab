Util 测试用例设计说明
=======
一、用例概述
--------
    场景说明：覆盖以下操作传入必带参数的主流程以及可选参数的返回验证
    * get_device
    * get_zone
    * resolve_ood
    * get_ood_status
    * get_noc_info
    * get_network_access_info
    * get_device_static_info
    * get_system_info
    * get_version_info
    * build_dir_from_object_map
<u>用例路径：src\cyfs-stack-test-typescript\TestSuite\unittest_stack_NON_*</u>  
该模块测试场景当前依赖模拟器环境实现并运行

二、用例场景
--------

### 1、get_device
* get_device-正常调用runtime接口   
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用runtime协议栈调用接口
    预期结果：
    1、正常返回设备信息
* get_device-正常调用ood接口
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用ood协议栈调用接口
    预期结果：
    1、正常返回设备信息
* 传入target对象，正常获取设备信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target参数和其他必填参数调用接口
    预期结果：
    1、正常返回设备信息
* 传入dec_id对象，正常获取设备信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回设备信息
* 传入target和dec_id对象，正常获取设备信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target、dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回设备信息
### 2、get_zone
* get_zone-正常调用runtime接口 
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用runtime协议栈调用接口
    预期结果：
    1、正常返回信息
* get_zone-正常调用ood接口
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用ood协议栈调用接口
    预期结果：
    1、正常返回信息
* 传入target对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入dec_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入target和dec_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target、dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入object_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入object_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入object_raw对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入object_raw参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
### 3、resolve_ood
* resolve_ood-正常调用runtime接口 
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用runtime协议栈调用接口
    预期结果：
    1、正常返回信息
* resolve_ood-正常调用ood接口
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用ood协议栈调用接口
    预期结果：
    1、正常返回信息
* 传入target对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入dec_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入owner_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入owner_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入target和dec_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target、dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
### 4、get_ood_status
* get_ood_status-正常调用runtime接口 
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用runtime协议栈调用接口
    预期结果：
    1、正常返回信息
* 传入target对象，正常获取ood信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入dec_id对象，正常获取ood信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入target和dec_id对象，正常获取ood信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target、dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
### 5、get_noc_info
* get_noc_info-正常调用runtime接口 
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用runtime协议栈调用接口
    预期结果：
    1、正常返回信息
* get_noc_info-正常调用ood接口
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用ood协议栈调用接口
    预期结果：
    1、正常返回信息
* 传入target对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入dec_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入target和dec_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target、dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
### 6、get_network_access_info
* get_network_access_info-正常调用runtime接口
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用runtime协议栈调用接口
    预期结果：
    1、正常返回信息 
* get_network_access_info-正常调用ood接口
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用ood协议栈调用接口
    预期结果：
    1、正常返回信息
* 传入target对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入dec_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入target和dec_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target、dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
### 7、get_device_static_info
* get_device_static_info-正常调用runtime接口 
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用runtime协议栈调用接口
    预期结果：
    1、正常返回信息
* get_device_static_info-正常调用ood接口
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用ood协议栈调用接口
    预期结果：
    1、正常返回信息
* 传入target对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入dec_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入target和dec_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target、dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
### 8、get_system_info
* get_system_info-正常调用runtime接口 
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用runtime协议栈调用接口
    预期结果：
    1、正常返回信息
* get_system_info-正常调用ood接口
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用ood协议栈调用接口
    预期结果：
    1、正常返回信息
* 传入target对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入dec_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入target和dec_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target、dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
### 9、get_version_info
* get_version_info-正常调用runtime接口 
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用runtime协议栈调用接口
    预期结果：
    1、正常返回信息
* get_version_info-正常调用ood接口
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入有效参数使用ood协议栈调用接口
    预期结果：
    1、正常返回信息
* 传入target对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入dec_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
* 传入target和dec_id对象，正常获取信息
    ```
    前置条件：
    1、准备好传参数据和传参对象类型无误
    2、模拟机或真机环境正常
    操作步骤：
    1、正常传入target、dec_id参数和其他必填参数调用接口
    预期结果：
    1、正常返回信息
















